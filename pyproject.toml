[project]
name = "chat-memory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.26.0",
    "sqlite-vec>=0.1.6",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
